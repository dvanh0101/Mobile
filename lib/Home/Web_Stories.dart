import 'package:flutter/material.dart';
class web_stories extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('web_stories', style: TextStyle(fontSize: 24)),
    );
  }
}